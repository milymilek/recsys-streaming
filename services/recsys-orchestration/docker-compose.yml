version: '3'

x-spark-common: &spark-common
  image: bitnami/spark:3.5.3
  volumes:
    - .datalake:/opt/bitnami/spark/.datalake
    - jobs:/opt/bitnami/spark/jobs
  networks:
    - recsys-streaming-net
  environment:
    - SPARK_SUBMIT_ARGS=--packages io.delta:delta-core_2.12:2.4.0
  user: root

x-airflow-common: &airflow-common
  build:
    context: .
    dockerfile: Dockerfile
  env_file:
    - airflow.env
  volumes:
    - .datalake:/opt/airflow/.datalake
    - jobs:/opt/airflow/jobs
    - dags:/opt/airflow/dags
    - logs:/opt/airflow/logs
  depends_on:
    - postgres
  networks:
    - recsys-streaming-net

services:
  spark-master:
    <<: *spark-common
    command: bin/spark-class org.apache.spark.deploy.master.Master
    ports:
      - "9090:8080"
      - "7077:7077"

  spark-worker:
    <<: *spark-common
    command: bin/spark-class org.apache.spark.deploy.worker.Worker spark://spark-master:7077
    depends_on:
      - spark-master
    environment:
      SPARK_MODE: worker
      SPARK_WORKER_CORES: 6
      SPARK_WORKER_MEMORY: 12g
      SPARK_MASTER_URL: spark://spark-master:7077

  postgres:
    image: postgres:14.0
    environment:
      - POSTGRES_USER=airflow
      - POSTGRES_PASSWORD=airflow
      - POSTGRES_DB=airflow
    networks:
      - recsys-streaming-net

  webserver:
    <<: *airflow-common
    command: webserver
    ports:
      - "8080:8080"
    depends_on:
      - scheduler

  scheduler:
    <<: *airflow-common
    command: bash -c "airflow db migrate && airflow users create --username admin --firstname Bart --lastname Marszalek --role Admin --email bartosz.j.marszalek@gmail.com --password admin && airflow scheduler"

networks:
  recsys-streaming-net: